version: '2'

services:
  isbpanel-frontend-backend:
    container_name: isbpanel-frontend-backend
    image: isbpanel-frontend-backend:0.0.1
    build: .
    volumes:
    - ./frontend:/app/frontend
    ports:
      - 1011:1011
    restart: always